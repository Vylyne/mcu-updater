{
    "_comment": "System packages Moonraker's update manager installs for us, named by moonraker-update-manager.conf. python3-serial is not for the agent - that is stdlib-only - it is for the display discovery helper, which runs the *system* python3 as a subprocess to ask the screens which they are. tests/test_repo_hygiene.py checks the conf still points here and that this still declares it.",
    "debian": [
        "python3-serial"
    ]
}
